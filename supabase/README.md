# Optional: deep-merge clap / pack keys if you prefer server merge later.
# v1 stores guest inputs as unique top-level keys (in_<id>), which already
# shallow-merge with the existing patch_room_game_state RPC. No SQL change required.
